<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Chess Online</title>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Space Grotesk', sans-serif;
      background: #18181b;
      min-height: 100vh;
      color: #ffffff;
    }

    .container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 100vh;
      align-items: center;
      padding: 2rem;
      gap: 4rem;
    }

    .content {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }

    .main-title {
      font-family: 'Space Grotesk', sans-serif;
      font-size: clamp(3rem, 8vw, 7rem);
      font-weight: 600;
      color: #ffffff;
      text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
      margin-bottom: 1rem;
      letter-spacing: -0.04em;
      line-height: 0.9;
    }

    .subtitle {
      font-size: clamp(1.2rem, 3vw, 2rem);
      color: #a1a1aa;
      margin-bottom: 3rem;
      font-weight: 400;
      letter-spacing: 0.02em;
    }

    .play-button {
      background: #ffffff;
      border: none;
      padding: 1.2rem 2.5rem;
      border-radius: 8px;
      font-size: 1.1rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.2s ease;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      align-self: flex-start;
    }

    .play-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
      background: #f4f4f5;
    }

    .play-button:active {
      transform: translateY(0);
    }

    .play-button a {
      text-decoration: none;
      color: #18181b;
      font-weight: 500;
    }

    .image-section {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .chess-piece {
      width: clamp(250px, 35vw, 400px);
      height: auto;
      transform: rotate(-15deg);
      filter: drop-shadow(0 10px 30px rgba(0, 0, 0, 0.3));
      transition: transform 0.3s ease;
    }

    .chess-piece:hover {
      transform: rotate(-10deg) scale(1.05);
    }

    .footer {
      grid-column: 1 / 3;
      text-align: center;
      color: #71717a;
      font-size: 0.9rem;
      padding: 2rem;
      border-top: 1px solid #27272a;
      margin-top: auto;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .container {
        grid-template-columns: 1fr;
        grid-template-rows: 1fr auto;
        padding: 1rem;
        text-align: center;
        position: relative;
      }

      .content {
        grid-column: 1;
        grid-row: 1;
        z-index: 2;
        position: relative;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        min-height: 80vh;
      }

      .image-section {
        grid-column: 1;
        grid-row: 1;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 1;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .chess-piece {
        width: clamp(200px, 60vw, 350px);
        opacity: 0.3;
        transform: rotate(-15deg);
      }

      .footer {
        grid-column: 1;
        grid-row: 2;
        z-index: 2;
        position: relative;
        margin-top: 0;
      }

      .play-button {
        z-index: 3;
        position: relative;
        align-self: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <h1 class="main-title">Chess Online</h1>
      <p class="subtitle">Experience the thrill of multiplayer</p>
      <button class="play-button">
        <a href="/play">Play with Randoms</a>
      </button>
    </div>
    
    <div class="image-section">
      <img src="/images/img1.png" alt="Chess Queen" class="chess-piece">
    </div>
    
    <div class="footer">
      <br>Challenge players worldwide • Master the game of kings</br>
      <br>© 2025 Chess Online</br>

    </div>
  </div>
</body>
</html>